# SimpleMarkdownExtensionCLI
An extension for SimpleMarkdown to add renderer for CLI output.

Installation
------------

```elixir
def deps do
    [{ :simple_markdown_extension_cli, "~> 0.1.2" }]
end
```
